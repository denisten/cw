export enum TaskWrapperHeight {
  opened = 149,
  closed = 64,
}

export enum TitleMarginLeft {
  inTowerInfo = 16,
  notInTowerInfo = 14,
}

export enum TitleWidth {
  inTowerInfo = 124,
  notInTowerInfo = 274,
}
export enum TitleMarginRight {
  inTowerInfo = 19,
  notInTowerInfo = 45,
}
